

- update skill.md with associated script
- can we add an uninstall script ? could we remove also all cached model too ?

should we create an agent-skill project ?
should we rename our skill with a folder datalab (as it source came from ?)
